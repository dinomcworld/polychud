# Core (required)
DISCORD_TOKEN=
DISCORD_CLIENT_ID=
DATABASE_URL=postgresql://bot:password@db:5432/polymarket_bot
NODE_ENV=production

# PostgreSQL (used by docker-compose)
POSTGRES_DB=polymarket_bot
POSTGRES_USER=bot
POSTGRES_PASSWORD=password

# Polymarket API endpoints
POLYMARKET_GAMMA_URL=https://gamma-api.polymarket.com
POLYMARKET_CLOB_URL=https://clob.polymarket.com

# Polling
POLL_CYCLE_MS=3600000
RESOLUTION_CHECK_CRON=*/30 * * * *
ON_DEMAND_CACHE_TTL_MS=60000
CLOSE_BET_PRICE_MAX_AGE_MS=30000

# Points economy (defaults, overridable per guild)
DEFAULT_STARTING_POINTS=1000
DEFAULT_MAX_BET=500
DEFAULT_MIN_BET=10
DEFAULT_DAILY_BONUS=100
MAX_ACTIVE_BETS_PER_USER=10
MAX_ACTIVE_BETS_PER_MARKET=3

# Book-quality gates. Bets are priced off the real order book (entry at the ask,
# exit at the bid), so markets whose book is too wide or too thin to produce a
# meaningful quote are refused rather than priced off a fictitious midpoint.
MAX_BET_SPREAD=0.08
MIN_MARKET_LIQUIDITY=500

# Logging
LOG_LEVEL=info
